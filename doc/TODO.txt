TODO Roadmap

+ = new feature, b = bug, r = refactoring
v = visual, d = documentation, t = test

---------TODO IN NEXT REALEASE 0.0.4
+ crack tools
+ connect to some hashcat api ?

---------TODO IN NEXT REALEASE 0.0.3
+ automatization to run it on mobile
+:Country specific p3wifi:
     - add tool to
        - downloadn and load database
        - cut only one country database
        - add warning messages - database not accesible etc.
        - add manual to this
     -> some SQL indexes for big data

---------TODO IN NEXT REALEASE 0.0.2
+: addc check funxtions for input + unit tests

t: search test
t: selenium tests ???
b: some tools operation depends on self.SOURCE_NAME, some a depends on file name
+: show cracked count in statDiv
d: documnet that .ini config have to be same like tool name (and chekc it with tests)!

+: add help small windows if i focus on button in tools (or just add icons)

------- DONE in REALEASE 0.0.2
+: add load backup in tools
+: create backup tool
t: behave testing
v: tools few visual changes

-------CREATE RELEASE

v: add click cords to map view
t: test table_v0 - duplicates
v: for same wifi with changed passwords/multiple sources are generated multiple points
+: dependencies to enable/disale to disable all what importparentplugin
+ pwncrack download handshakes
+: more wpa_sec/pwncrack/wigle keys
d: documentation
    d: diagramy, ispirovat se tady https://docs.oracle.com/en/database/oracle/oracle-database/19/sqlrf/CREATE-TABLE.html
+ sources to auto install
b: czech characters are saved to database badly
v+: different colors for different sources
b: handshakes - fix loading from pcapng files - encryption not working
b: repait dynamiccaly loading (Load square button)
+: create UML diagrams for plugins (find way to auto documenting?)
b: fix header compability with window resizing
+: add reset default values in tools
r: refactor external libraries to be sure not sending data to fucking google etc...
