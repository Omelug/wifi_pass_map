TODO Roadmap

+ = new feature, b = bug, r = refactoring
v = visual, d = documentation, t = test

--------- CHANGELOG REALASE 0.0.1 (already ok)
+ pwncrack
r: python type control
+ add git hook for unit tests
+ tablev0 config to disable duplicates
b: change example.d and add description to README
b: repair Table_v0 source invalid on map
+: wigle - add time, And check only older_than (WIGLE CONFIG)
r : delete TABLE_NAME, use SOURCE_NAME
+: sourcce order
+: global config
+ add source on/off and source order in menu
+: example for tablev0
b: global config to change init map ords
r: add _ to private methods
+: regex serach for ESSIDs

---------TODO IN NEXT REALEASE 0.0.1
t: test table_v0 - duplicates
+ add config to check what shoud be done with duplicates (merge/remote_old/ignore_new)
v: for same wifi with changed passwords/multiple sources are generated multiple points

-------CREATE RELEASE
b: some tools operation depends on self.SOURCE_NAMEsoem a depends on file name
+: dependencies to enable/disale to disable all what importparentplugin
+ pwncrack download handshakes
+: more wpa_sec/pwncrack/wigle keys
d: documentation
    d: diagramy, ispirovat se tady https://docs.oracle.com/en/database/oracle/oracle-database/19/sqlrf/CREATE-TABLE.html
+ sources to auto install
b: czech characters are saved to database badly
+ add info button text to tun tools button
+ create pwncrack-wpa-sec/potfile change
v+: different colors for different sources
b: handshakes - fix loading from pcapng files - dont work ncryption
b: repait dynamiccaly loading (Load square button)
+: create UML diagram (find way to auto documenting?)
+: show cracked count in statDiv
b: fix header compability with window resizing
+: add help small windows if i focus on button in tools (or just add icons)
+: add reset default values in tools
r: refactor external libraries to be sure not sending data to fucking gooogle etc...
+:Country specific p3wifi:
     - add tool to
        - downloadn and load database
        - cut only one database
        - add warning messages - database not accesible etc.
        - add manual to this


