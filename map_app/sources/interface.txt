if is module loaded,

- it creates new table in database during import if nessasary
- it creates new config file in map_app
def get_map_data(filters) -> list

set of tools for module

get_tools() ->

